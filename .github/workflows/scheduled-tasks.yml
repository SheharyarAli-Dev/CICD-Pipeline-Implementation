name: Scheduled Tasks

on:
  schedule:
    # Runs every day at midnight (UTC)
    - cron: '0 0 * * *'

  # Allow manual trigger as well
  workflow_dispatch:

jobs:
  maintenance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run scheduled maintenance script
        run: |
          echo "🧹 Running daily maintenance tasks..."
          echo "Date: $(date)"
          # Example: npm run backup or a custom script
          echo "Performing backup..."
          # Replace below line with your actual maintenance command
          echo "Maintenance complete ✅"

      - name: Send notification (optional)
        if: always()
        run: |
          echo "Sending notification to admin..."
          # You can integrate with Discord, Slack, or Email APIs here
